#3 - Dado um vetor de tamanho N, inverta a ordem de seus elementos e imprima o vetor resultante.

def lista(lista,tamanho):
  for i in range(tamanho):
    print('valor',i+1,':')
    lista[i] = int(input())

def inversão(lista,tamanho):
  for i in range(n):
    num = lista1.pop()
    lista2.append(num)

n = int(input('digite o total de numeros para reversão: '))
lista1 = [0] * n
lista(lista1,n)
print(lista1)

lista2 = []
inversão(lista1,n)
print(lista2)